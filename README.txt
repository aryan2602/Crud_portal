#  database name : crudportal ; 
#  database setting file : backphp/config.php ; 
#  all php file are in backphp folder ; 
#  database sql file is in database folder ; 
#  upload have all uploaded images ; 

#  bootstrap 4 used for styling and frondend cdn links are in index file;